import { UploadCloud } from 'lucide-react'
function Field({ label, children }) { return <label className="form-field"><span>{label}</span>{children}</label> }
function BookingForm({ form, errors, onChange, onSubmit }) { const today = new Date().toISOString().split('T')[0]; return <form className="booking-form" onSubmit={onSubmit} noValidate><div className="form-grid"><Field label="Customer name"><input name="name" value={form.name} onChange={onChange} placeholder="Enter your full name" />{errors.name && <small>{errors.name}</small>}</Field><Field label="Mobile number"><input name="mobile" value={form.mobile} onChange={onChange} inputMode="numeric" maxLength="10" placeholder="10-digit mobile number" />{errors.mobile && <small>{errors.mobile}</small>}</Field></div><Field label="Delivery address"><textarea name="address" value={form.address} onChange={onChange} rows="3" placeholder="House / flat number, area and landmark" />{errors.address && <small>{errors.address}</small>}</Field><div className="form-grid"><Field label="Rental start date"><input type="date" name="rentalStartDate" value={form.rentalStartDate} onChange={onChange} min={today} />{errors.rentalStartDate && <small>{errors.rentalStartDate}</small>}</Field><Field label="Number of PS5 consoles"><select name="quantity" value={form.quantity} onChange={onChange}><option value="1">1 PS5</option><option value="2">2 PS5s</option><option value="3">3 PS5s</option></select></Field></div><Field label="ID proof (JPG, JPEG or PNG · Max 5 MB)"><span className="upload-box"><UploadCloud size={23} /><span>{form.idProof?.name || 'Choose a photo of your ID'}</span><input type="file" name="idProof" accept="image/jpeg,image/png" onChange={onChange} /></span>{errors.idProof && <small>{errors.idProof}</small>}</Field><Field label="Additional notes (optional)"><textarea name="notes" value={form.notes} onChange={onChange} rows="3" placeholder="Anything we should know?" /></Field><button className="button button-primary submit-button" type="submit">Submit Booking Request</button></form> }
export default BookingForm
